<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

    </data>

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <LinearLayout
                android:id="@+id/linearlayout"
                android:layout_width="match_parent"
                android:layout_height="@dimen/_85sdp"
                android:orientation="horizontal">

                <TextView
                    android:id="@+id/consumption_material"
                    style="@style/DetailsDialogTextStyle"
                    android:layout_height="match_parent"
                    android:text="@string/material"
                    android:textColor="@color/primary" />

                <TextView
                    android:id="@+id/consumption_available"
                    style="@style/DetailsDialogTextStyle"
                    android:layout_height="match_parent"
                    android:text="@string/available"
                    android:textColor="@color/weird_green" />

                <TextView
                    android:id="@+id/consumption_then"
                    style="@style/DetailsDialogTextStyle"
                    android:layout_height="match_parent"
                    android:text="@string/then"
                    android:textColor="@color/coral" />

                <TextView
                    android:id="@+id/consumption_batches"
                    style="@style/DetailsDialogTextStyle"
                    android:layout_height="match_parent"
                    android:text="@string/batches"
                    android:textColor="@color/un_active_primary" />

            </LinearLayout>

            <View
                android:layout_width="match_parent"
                android:layout_height="@dimen/_1sdp"
                android:layout_marginStart="@dimen/_48sdp"
                android:layout_marginTop="@dimen/_87sdp"
                android:layout_marginEnd="@dimen/_48sdp"
                android:background="#A4CACAE1" />

        </RelativeLayout>

    </androidx.cardview.widget.CardView>
</layout>